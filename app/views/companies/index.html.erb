<p id="notice"><%= notice %></p>

<h1>Companies</h1>


<h1>Create Nueva Empresa:</h1>
<%= render 'form', company: @new_company %>


<table id="companies_table">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody id="companies_table_body">
    <% @companies.each do |company| %>
      <%= render company %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Company', new_company_path %>
